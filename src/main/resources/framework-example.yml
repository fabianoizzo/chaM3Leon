batch:
  spark:
    app-name: "batch"
    master: "yarn"
    deploy-mode: "client"
    interval-sec: 600
    num-executors: 2
    executor-cores: 2
    executor-memory: "2g"
    driver-memory: "1g"
    other-config:
      spark-config:
    
  kafka:
    broker: "kafka1:9092"
    topics:
      topic1:
        name:
        class:
      topic2:
        name:
        class:
  hdfs:
    data-directory:
    
  cassandra:

speed:
  spark:
  kafka:
  cassandra:

ml:
  spark:
  hdfs:
  cassandra:
